name: FCI-07 Functional Security

# FCI-07 owns the authorization-negative journeys and runners only.
# PR functional-fast routing/required-check wiring is owned by FCI-08 (#605),
# while main/full/nightly topology is owned by FCI-09 (#611).
# This placeholder intentionally contains no executable triggers or jobs so
# those gate owners remain the single source of CI status semantics.
