name: retired-write-lane

on:
  workflow_run:
    workflows: ["External PR review signal"]
    types: [completed]
  workflow_dispatch:

permissions: {}

jobs:
  evaluate:
    runs-on: ubuntu-latest
    steps:
      - run: echo "privileged status publisher retired"
