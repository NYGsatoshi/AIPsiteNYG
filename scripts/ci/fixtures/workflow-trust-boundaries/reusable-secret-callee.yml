name: reusable secret callee
on:
  workflow_call:
permissions: {}
jobs:
  inner:
    runs-on: ubuntu-latest
    environment: licensed
    env:
      LICENSE: ${{ secrets.LICENSE }}
    steps:
      - run: test -n "$LICENSE"
