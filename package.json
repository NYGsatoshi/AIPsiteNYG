{
  "name": "aipsite-nyg-ui-tests",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "dotnet build AipPortal.slnx --configuration Release --no-restore",
    "test": "playwright test",
    "test:ui": "playwright test"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.2",
    "@playwright/test": "^1.54.2",
    "http-server": "^14.1.1"
  }
}
