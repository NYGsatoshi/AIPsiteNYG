jobs:
  - job: FrontendCore
    displayName: Angular build, unit, quality and Storybook
    dependsOn: DetectChanges
    condition: and(succeeded(), eq(dependencies.DetectChanges.outputs['changes.primary'], 'true'))
    timeoutInMinutes: 60
    variables:
      NODE_OPTIONS: --max-old-space-size=4096
    steps:
      - checkout: self
        persistCredentials: false
      - task: NodeTool@0
        displayName: Install Node.js 24
        inputs:
          versionSpec: '24.x'
      - bash: |
          mkdir -p artifacts/frontend-inspections
          bash scripts/ci/npm-ci-retry.sh .
        displayName: Install repository dependencies
      - bash: bash scripts/ci/npm-ci-retry.sh frontend
        displayName: Install Angular dependencies
        condition: succeededOrFailed()
      - bash: |
          set -euo pipefail
          for attempt in 1 2; do
            set +e
            if [ "$attempt" -eq 1 ]; then
              npm --prefix tools/frontend-inspections install --prefer-offline --ignore-scripts --no-audit --no-fund
            else
              npm_config_prefer_offline=false npm --prefix tools/frontend-inspections install --prefer-online --ignore-scripts --no-audit --no-fund
            fi
            status=$?
            set -e
            [ "$status" -eq 0 ] && exit 0
            [ "$attempt" -eq 2 ] && exit "$status"
            sleep 10
          done
        displayName: Install frontend inspection toolchain
        condition: succeededOrFailed()
      - bash: npm run build
        workingDirectory: frontend
        displayName: Angular production build
        condition: succeededOrFailed()
      - bash: |
          set -euo pipefail
          test -n "${SYNCFUSION_LICENSE:-}" || {
            echo "SYNCFUSION_LICENSE must be configured as a secret Azure Pipeline variable." >&2
            exit 1
          }
          test -x node_modules/.bin/syncfusion-license
          npm run build:licensed
        workingDirectory: frontend
        displayName: Validate licensed frontend build
        condition: succeededOrFailed()
        env:
          SYNCFUSION_LICENSE: $(SYNCFUSION_LICENSE)
      - bash: npm run test
        workingDirectory: frontend
        displayName: Angular unit tests
        condition: succeededOrFailed()
      - bash: |
          printf '{"status":"inspection-started","workflow":"azure-pipelines"}\n' > artifacts/frontend-inspections/ci-manifest.json
          npm run lint:frontend
        displayName: Run complete frontend inspection inventory
        condition: succeededOrFailed()
      - bash: ./node_modules/.bin/tsc --noEmit --project tsconfig.app.json
        workingDirectory: frontend
        displayName: Validate TypeScript types
        condition: succeededOrFailed()
      - bash: |
          npm run check:architecture
          npm run test:architecture
          npm run test:syncfusion-license
        workingDirectory: frontend
        displayName: Validate frontend architecture and license safeguards
        condition: succeededOrFailed()
      - bash: npm run build-storybook
        workingDirectory: frontend
        displayName: Angular Storybook build
        condition: succeededOrFailed()
        env:
          NODE_OPTIONS: --max-old-space-size=4096
      - task: PublishPipelineArtifact@1
        displayName: Publish frontend inspection reports
        condition: always()
        inputs:
          targetPath: '$(Build.SourcesDirectory)/artifacts/frontend-inspections'
          artifact: frontend-inspection-reports
          publishLocation: pipeline

  - job: FrontendPlaywright
    displayName: Angular Playwright smoke
    dependsOn: DetectChanges
    condition: and(succeeded(), eq(dependencies.DetectChanges.outputs['changes.primary'], 'true'))
    timeoutInMinutes: 60
    steps:
      - checkout: self
        persistCredentials: false
      - task: NodeTool@0
        inputs:
          versionSpec: '24.x'
        displayName: Install Node.js 24
      - bash: |
          mkdir -p playwright-report test-results
          bash scripts/ci/npm-ci-retry.sh .
        displayName: Install repository dependencies
      - bash: bash scripts/ci/npm-ci-retry.sh frontend
        displayName: Install Angular dependencies
      - bash: npm run test:ui:angular:docker
        displayName: Run Angular Playwright smoke
      - task: PublishPipelineArtifact@1
        condition: always()
        inputs:
          targetPath: '$(Build.SourcesDirectory)/playwright-report'
          artifact: angular-playwright-report
          publishLocation: pipeline
        displayName: Publish Playwright report
      - task: PublishPipelineArtifact@1
        condition: always()
        inputs:
          targetPath: '$(Build.SourcesDirectory)/test-results'
          artifact: angular-playwright-results
          publishLocation: pipeline
        displayName: Publish Playwright results

  - job: RealBackendP0
    displayName: Real Backend P0 acceptance
    dependsOn: DetectChanges
    condition: and(succeeded(), eq(dependencies.DetectChanges.outputs['changes.p0'], 'true'))
    timeoutInMinutes: 45
    steps:
      - checkout: self
        persistCredentials: false
      - task: NodeTool@0
        inputs:
          versionSpec: '24.x'
        displayName: Install Node.js 24
      - bash: |
          mkdir -p playwright-report test-results
          bash scripts/ci/npm-ci-retry.sh .
        displayName: Install smoke runner dependencies
      - bash: |
          set -euo pipefail
          test -n "${SYNCFUSION_LICENSE:-}" || {
            echo "SYNCFUSION_LICENSE is not configured." >&2
            exit 1
          }
          node tests/ui/run-real-backend-p0.mjs
        displayName: Run required real-backend P0 acceptance
        env:
          SYNCFUSION_LICENSE: $(SYNCFUSION_LICENSE)
      - task: PublishPipelineArtifact@1
        condition: always()
        inputs:
          targetPath: '$(Build.SourcesDirectory)/test-results'
          artifact: real-backend-p0-results
          publishLocation: pipeline
        displayName: Publish P0 evidence
      - task: PublishPipelineArtifact@1
        condition: always()
        inputs:
          targetPath: '$(Build.SourcesDirectory)/playwright-report'
          artifact: real-backend-p0-playwright
          publishLocation: pipeline
        displayName: Publish P0 Playwright report

  - job: RealBackendMyTasks
    displayName: Real Backend My Tasks acceptance
    dependsOn: DetectChanges
    condition: and(succeeded(), eq(dependencies.DetectChanges.outputs['changes.myTasks'], 'true'))
    timeoutInMinutes: 45
    steps:
      - checkout: self
        persistCredentials: false
      - task: NodeTool@0
        inputs:
          versionSpec: '24.x'
        displayName: Install Node.js 24
      - bash: |
          mkdir -p playwright-report test-results
          bash scripts/ci/npm-ci-retry.sh .
        displayName: Install smoke runner dependencies
      - bash: |
          set -euo pipefail
          test -n "${SYNCFUSION_LICENSE:-}" || {
            echo "SYNCFUSION_LICENSE is not configured." >&2
            exit 1
          }
          node tests/ui/run-real-backend-my-tasks.mjs
        displayName: Run required My Tasks real-backend acceptance
        env:
          SYNCFUSION_LICENSE: $(SYNCFUSION_LICENSE)
      - task: PublishPipelineArtifact@1
        condition: always()
        inputs:
          targetPath: '$(Build.SourcesDirectory)/test-results'
          artifact: real-backend-my-tasks-results
          publishLocation: pipeline
        displayName: Publish My Tasks evidence
      - task: PublishPipelineArtifact@1
        condition: always()
        inputs:
          targetPath: '$(Build.SourcesDirectory)/playwright-report'
          artifact: real-backend-my-tasks-playwright
          publishLocation: pipeline
        displayName: Publish My Tasks Playwright report

  - job: MBJ01
    displayName: MBJ-01 fresh bootstrap acceptance
    dependsOn: DetectChanges
    condition: and(succeeded(), eq(dependencies.DetectChanges.outputs['changes.mbj01'], 'true'))
    timeoutInMinutes: 45
    steps:
      - checkout: self
        persistCredentials: false
      - task: NodeTool@0
        inputs:
          versionSpec: '24.x'
        displayName: Install Node.js 24
      - bash: |
          mkdir -p test-results
          bash -n scripts/ci/run-mbj01-bootstrap-acceptance.sh
          node --check tests/ui/mbj01-bootstrap-acceptance.mjs
        displayName: Validate MBJ-01 runner syntax
      - bash: |
          set -euo pipefail
          test -n "${SYNCFUSION_LICENSE:-}" || { echo "SYNCFUSION_LICENSE is not configured." >&2; exit 1; }
          bash scripts/ci/run-mbj01-bootstrap-acceptance.sh
        displayName: Run MBJ-01 acceptance
        env:
          SYNCFUSION_LICENSE: $(SYNCFUSION_LICENSE)
      - task: PublishPipelineArtifact@1
        condition: always()
        inputs:
          targetPath: '$(Build.SourcesDirectory)/test-results'
          artifact: mbj01-evidence
          publishLocation: pipeline
        displayName: Publish MBJ-01 evidence

  - job: MBJ02
    displayName: MBJ-02 invite onboarding acceptance
    dependsOn: DetectChanges
    condition: and(succeeded(), eq(dependencies.DetectChanges.outputs['changes.mbj02'], 'true'))
    timeoutInMinutes: 45
    steps:
      - checkout: self
        persistCredentials: false
      - task: NodeTool@0
        inputs:
          versionSpec: '24.x'
        displayName: Install Node.js 24
      - bash: |
          mkdir -p test-results
          bash -n scripts/ci/run-mbj02-invite-acceptance.sh
          node --check tests/ui/mbj02-invite-acceptance.mjs
        displayName: Validate MBJ-02 runner syntax
      - bash: |
          set -euo pipefail
          test -n "${SYNCFUSION_LICENSE:-}" || { echo "SYNCFUSION_LICENSE is not configured." >&2; exit 1; }
          bash scripts/ci/run-mbj02-invite-acceptance.sh
        displayName: Run MBJ-02 acceptance
        env:
          SYNCFUSION_LICENSE: $(SYNCFUSION_LICENSE)
      - task: PublishPipelineArtifact@1
        condition: always()
        inputs:
          targetPath: '$(Build.SourcesDirectory)/test-results'
          artifact: mbj02-evidence
          publishLocation: pipeline
        displayName: Publish MBJ-02 evidence

  - job: MBJ03
    displayName: MBJ-03 session lifecycle acceptance
    dependsOn: DetectChanges
    condition: and(succeeded(), eq(dependencies.DetectChanges.outputs['changes.mbj03'], 'true'))
    timeoutInMinutes: 45
    steps:
      - checkout: self
        persistCredentials: false
      - task: NodeTool@0
        inputs:
          versionSpec: '24.x'
        displayName: Install Node.js 24
      - bash: |
          mkdir -p test-results
          bash -n scripts/ci/run-mbj03-session-acceptance.sh
          node --check tests/ui/mbj03-session-acceptance.mjs
        displayName: Validate MBJ-03 runner syntax
      - bash: |
          set -euo pipefail
          test -n "${SYNCFUSION_LICENSE:-}" || { echo "SYNCFUSION_LICENSE is not configured." >&2; exit 1; }
          bash scripts/ci/run-mbj03-session-acceptance.sh
        displayName: Run MBJ-03 acceptance
        env:
          SYNCFUSION_LICENSE: $(SYNCFUSION_LICENSE)
      - task: PublishPipelineArtifact@1
        condition: always()
        inputs:
          targetPath: '$(Build.SourcesDirectory)/test-results'
          artifact: mbj03-evidence
          publishLocation: pipeline
        displayName: Publish MBJ-03 evidence
