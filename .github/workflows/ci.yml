name: CI

on:
  push:
    branches: ["main"]
  pull_request:

jobs:
  build-test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:18-alpine
        env:
          POSTGRES_DB: aip_portal_ci
          POSTGRES_USER: aip_portal_ci
          POSTGRES_PASSWORD: aip_portal_ci_password
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U aip_portal_ci -d aip_portal_ci"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      ASPNETCORE_ENVIRONMENT: Test
      ConnectionStrings__DefaultConnection: Host=localhost;Port=5432;Database=aip_portal_ci;Username=aip_portal_ci;Password=aip_portal_ci_password
      POSTGRES_TEST_CONNECTION_STRING: Host=localhost;Port=5432;Database=aip_portal_ci;Username=aip_portal_ci;Password=aip_portal_ci_password

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: npm

      - name: Restore
        run: dotnet restore AipPortal.slnx

      - name: Build
        run: dotnet build AipPortal.slnx --configuration Release --no-restore

      - name: Restore .NET tools
        run: dotnet tool restore

      - name: Apply PostgreSQL migrations
        run: dotnet ef database update --project src/AipPortal.Infrastructure --startup-project src/AipPortal.Web --configuration Release

      - name: Test
        run: dotnet test AipPortal.slnx --configuration Release --no-build --verbosity normal

      - name: Install frontend test dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: UI tests
        run: npm run test:ui

      - name: Upload Playwright artifacts
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-ui-test-artifacts
          path: |
            playwright-report/
            test-results/
          if-no-files-found: ignore

      - name: PostgreSQL logs on failure
        if: failure()
        run: docker logs ${{ job.services.postgres.id }}

  security-scan:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      IMAGE_NAME: aipsite-nyg-ci:${{ github.sha }}
      POSTGRES_PASSWORD: ci_only_compose_config_password

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Initialize .NET dependency scan reports
        run: |
          printf 'Vulnerable package scan did not run.\n' > dotnet-vulnerable-packages.txt
          printf 'Deprecated package scan did not run.\n' > dotnet-deprecated-packages.txt

      - name: Scan repository secrets with Gitleaks
        run: |
          set +e
          docker run --rm \
            -v "$PWD:/repo" \
            zricethezav/gitleaks:v8.24.3 \
            detect --no-git --source /repo --redact --verbose \
            --report-format json --report-path /repo/gitleaks-report.json
          gitleaks_status=$?
          if [ ! -f gitleaks-report.json ]; then
            printf '[]\n' > gitleaks-report.json
          fi
          cat gitleaks-report.json
          exit "$gitleaks_status"

      - name: Upload Gitleaks secret scan report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: gitleaks-secret-scan-report
          path: gitleaks-report.json
          if-no-files-found: error

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      - name: Restore
        run: dotnet restore AipPortal.slnx

      - name: Check .NET vulnerable packages
        run: |
          printf 'Vulnerable package scan started but did not complete.\n' > dotnet-vulnerable-packages.txt
          set +e
          dotnet list AipPortal.slnx package --vulnerable --include-transitive > dotnet-vulnerable-packages.txt 2>&1
          vulnerable_status=$?
          set -e
          if [ ! -s dotnet-vulnerable-packages.txt ]; then
            echo "dotnet vulnerable package report was not generated." > dotnet-vulnerable-packages.txt
          fi
          cat dotnet-vulnerable-packages.txt
          if grep -q "has the following vulnerable packages" dotnet-vulnerable-packages.txt; then
            echo "Vulnerable .NET packages were detected. Review dotnet-vulnerable-packages.txt."
            exit 1
          fi
          if [ "$vulnerable_status" -ne 0 ]; then
            echo "dotnet vulnerable package scan failed."
            exit "$vulnerable_status"
          fi

      - name: Report .NET deprecated packages
        run: |
          printf 'Deprecated package scan started but did not complete.\n' > dotnet-deprecated-packages.txt
          set +e
          dotnet list AipPortal.slnx package --deprecated > dotnet-deprecated-packages.txt 2>&1
          deprecated_status=$?
          set -e
          if [ ! -s dotnet-deprecated-packages.txt ]; then
            echo "dotnet deprecated package report was not generated." > dotnet-deprecated-packages.txt
          fi
          cat dotnet-deprecated-packages.txt
          if grep -q "has the following deprecated packages" dotnet-deprecated-packages.txt; then
            echo "Deprecated .NET packages were detected. Review dotnet-deprecated-packages.txt."
            echo "Deprecated packages are report-only for the initial security baseline."
          fi
          if [ "$deprecated_status" -ne 0 ]; then
            echo "dotnet deprecated package scan failed."
            exit "$deprecated_status"
          fi

      - name: Upload .NET dependency scan reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: dotnet-dependency-scan-reports
          path: |
            dotnet-vulnerable-packages.txt
            dotnet-deprecated-packages.txt
          if-no-files-found: error

      - name: Validate Docker Compose configs
        env:
          POSTGRES_DB: aip_portal
          POSTGRES_USER: aip_portal
          POSTGRES_PASSWORD: ci_dummy_password
          LOCAL_ADMIN_PASSWORD: ci_dummy_local_admin_password
        run: |
          docker compose config --quiet
          docker compose -f docker-compose.local.yml config --quiet
          docker compose -f docker-compose.onprem.yml config --quiet

      - name: Initialize Trivy image scan report
        run: printf 'Trivy image scan did not run.\n' > trivy-image-report.txt

      - name: Build Docker image
        run: docker build --pull --tag "$IMAGE_NAME" .

      - name: Scan Docker image with Trivy
        run: |
          printf 'Trivy image scan started but did not complete.\n' > trivy-image-report.txt
          set +e
          docker run --rm \
            -v /var/run/docker.sock:/var/run/docker.sock \
            -v "$PWD:/repo" \
            aquasec/trivy:0.65.0 \
            image --scanners vuln --vuln-type os,library \
            --severity HIGH,CRITICAL --ignore-unfixed \
            --format table --output /repo/trivy-image-report.txt \
            "$IMAGE_NAME"
          trivy_status=$?
          if [ ! -s trivy-image-report.txt ]; then
            echo "No Trivy report was generated." > trivy-image-report.txt
          fi
          cat trivy-image-report.txt
          if [ "$trivy_status" -ne 0 ]; then
            echo "Trivy scan command failed; failing CI because the image scan did not complete."
            exit "$trivy_status"
          fi
          echo "Trivy HIGH/CRITICAL findings are report-only for the initial security baseline."

      - name: Upload Trivy image scan report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: trivy-image-scan-report
          path: trivy-image-report.txt
          if-no-files-found: warn
