name: invalid indexed secret
on:
  pull_request:
permissions: {}
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: consume indexed secret
        env:
          TOKEN: ${{ secrets['MY_TOKEN'] }}
        run: test -n "$TOKEN"
