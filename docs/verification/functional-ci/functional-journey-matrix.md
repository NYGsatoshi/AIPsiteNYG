# Functional CI journey matrix

Status: canonical P0/P1 user-journey inventory for Issue #577 / FCI-01.

Policy terms in this matrix are defined in
[`functional-test-policy.md`](./functional-test-policy.md). Gate execution and
release ownership are defined in
[`functional-gate-topology.md`](./functional-gate-topology.md).

## Reading the matrix

- **Owner** names the primary domain that is responsible for the journey.
- **Mock policy** is the maximum mock boundary allowed for owner credit.
- **Gates** are target Functional CI ownership, not a claim that every target
  workflow is already wired. FCI-08/09/10 implement the remaining topology.
- **Current mapping** records existing evidence. A mapping marked `focused only`
  does not satisfy the real-functional owner slot.
- **Status** describes whether an executable real owner exists on current
  `main`. It is not a historical CI result for a particular SHA.

Runtime budgets (`B1`-`B4`) are defined in the policy.

## P0 journeys

| Stable ID | User-visible goal | Preconditions / fixture | Positive path | Required negative path | Owner | Mock policy | Required dependencies | Expected gates | Existing test / script mapping | Budget | Current status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `FUNC-AUTH-001` | A user can authenticate, establish a valid session, use CSRF-protected mutations, and lose access when the session is invalidated | Synthetic `@example.test` account; migrated isolated DB; auth cookies + Data Protection state | CSRF token -> login -> `/api/auth/me` -> protected mutation/session use -> logout or current-session continuation | invalid/missing CSRF, anonymous access, cleared/logged-out/expired session | `auth/session/onboarding` | No mock/intercept of auth, CSRF, session store, or protected API | Angular/HTTP client where browser-owned; ASP.NET Core auth; Data Protection; PostgreSQL | `functional-fast` (representative), `functional-full`, `functional-extended`, projected to `functional-release` | `scripts/ci/run-mbj03-session-acceptance.sh`; `scripts/ci/run-mvp-a-authz-boundary-acceptance.sh`; `tests/ui/run-real-backend-p0.mjs` (manifest MBJ-03 baseline) | `B2` | `IMPLEMENTED` |
| `FUNC-AUTH-002` | An administrator can invite a user and the invited user can validate, accept, and then authenticate into the authorized Workspace | Synthetic admin and invitees; fresh migrated DB; deterministic invite fixture | admin creates invite -> anonymous validates -> accepts -> session/login succeeds | used/revoked/expired token, mismatched email, cross-Tenant invite rejection | `auth/session/onboarding` | No mock of invite/auth/Workspace membership APIs | ASP.NET Core invite/auth pipeline; PostgreSQL; CSRF | `functional-fast` when onboarding/auth changes, `functional-full`, `functional-extended` | `scripts/ci/run-mbj02-invite-acceptance.sh`; `tests/ui/mbj02-invite-acceptance.mjs` | `B2` | `IMPLEMENTED` |
| `FUNC-AUTH-003` | A fresh authorized deployment can bootstrap its first administrator and persist the default Workspace across restart | Fresh isolated DB; synthetic bootstrap admin; seed enabled only for this fixture | bootstrap -> login -> exactly one authorized default Workspace -> restart -> same persisted identity/Workspace | malformed/missing bootstrap fixture must fail closed; no password material in evidence | `auth/session/onboarding` | No mock of bootstrap/auth/Workspace persistence | ASP.NET Core startup seed; PostgreSQL; Data Protection | `functional-full`, `functional-extended` | `scripts/ci/run-mbj01-bootstrap-acceptance.sh`; `tests/ui/mbj01-bootstrap-acceptance.mjs` | `B3` | `IMPLEMENTED` |
| `FUNC-WS-001` | An authorized user can create/enter a Workspace and the resulting membership is enforced by the real backend | Synthetic authenticated user with delegated Workspace capability; isolated migrated DB | create Workspace -> membership/current Workspace visible -> reload/read persists | unauthorized/non-member Workspace access denied without metadata leakage | `workspace/membership` | No mock of Workspace, capability, membership, auth, or persistence APIs | production Angular for browser owner; ASP.NET Core; EF Core; PostgreSQL | `functional-fast`, `functional-full`, `functional-extended`, projected to `functional-release` | `tests/ui/run-real-backend-p0.mjs`: `creates and activates a Workspace through the delegated real-backend capability`; U-22 same-lineage journey; MBJ-01 supplies bootstrap persistence evidence | `B2` | `IMPLEMENTED` |
| `FUNC-PROJ-001` | An authorized Workspace member can create a Draft Project and explicitly activate/open it | Authorized Workspace/member; project-create capability; isolated migrated DB | create Draft Project -> activate -> open/read after reload | non-member or unauthorized activation/read denied; stale capability fails closed | `project/task/execution` | No mock of Project, activation, capability, auth, or persistence APIs | production Angular; ASP.NET Core; EF Core; PostgreSQL | `functional-fast`, `functional-full`, `functional-extended`, projected to `functional-release` | `tests/ui/run-real-backend-p0.mjs`: `creates a Draft Project and explicitly activates it through the real backend`; U-22 same-lineage journey | `B2` | `IMPLEMENTED` |
| `FUNC-TASK-001` | A user can create/open a Task in an authorized Project, mutate it through current authorization, and read the persisted state | Authorized active Project; synthetic task data; isolated migrated DB | Workspace -> Project -> create/open Task -> authorized mutation -> reload/read | revoked/non-member/foreign-scope Task access or mutation denied | `project/task/execution` | No mock of Project/Task/auth/persistence APIs | production Angular; ASP.NET Core; EF Core; PostgreSQL | `functional-fast`, `functional-full`, `functional-extended`, projected to `functional-release` | `tests/ui/run-real-backend-p0.mjs`; `TASK-V1-PR03C uses the real backend for task detail, mutations, revocation, and File grant reauthorization`; U-22 same-lineage journey | `B2` | `IMPLEMENTED` |
| `FUNC-TASK-002` | A user can use canonical My Tasks scopes, relationships, filters, and paging against current authorization | Seeded tasks across authorized/unauthorized scopes; migrated isolated DB | open My Tasks -> select scope/filter/page -> results/counts reflect PostgreSQL -> reload/re-query | revoked or unauthorized Project/Workspace items are absent/denied; no opaque protected identifiers leak | `project/task/execution` | No mock of My Tasks/auth/persistence owner APIs | production Angular; ASP.NET Core; EF Core; PostgreSQL | `functional-fast` when My Tasks changes, `functional-full`, `functional-extended` | `tests/ui/run-real-backend-my-tasks.mjs`; `scripts/ci/real-backend-my-tasks-required-tests.txt`: `TASK-V1-PR04 exercises canonical My Tasks scopes, relationships, filters, paging, and UI against PostgreSQL` | `B2` | `IMPLEMENTED` |
| `FUNC-EXEC-001` | A user can execute an authorized Task using eligible Project Files and read the same durable result after reload | Authorized Workspace/Project/Task; current clean Project File; first-party runtime; isolated migrated DB | configure/use Project Files scope -> one idempotent execution request -> persisted lifecycle -> durable result -> reload/reread | unauthorized Task, wrong-scope/revoked source, stale authority, replay/idempotency misuse denied or fail closed | `project/task/execution` | No mock of auth, source materialization, execution, result, or persistence APIs; approved first-party runtime is real app boundary | production Angular; ASP.NET Core; EF Core; PostgreSQL; Project File storage/materialization; runtime | `functional-fast` representative slice when execution changes, `functional-full`, `functional-extended`, projected to `functional-release` | #464 Compose-backed Task execution Golden Path; `tests/ui/real-backend-smoke.spec.ts` real execution coverage; #481 is downstream public-HTTPS projection | `B3` | `IMPLEMENTED` |
| `FUNC-FILE-001` | A File grant/download or Task-source access is reauthorized at use time and follows current membership/scope | Synthetic clean File in authorized Project plus revoked/foreign File identifiers | obtain/use current authorized File capability -> operation succeeds -> current Task/source association remains usable | revoked/inaccessible/foreign-scope File denied without protected metadata; stale grant cannot bypass current auth | `files` | No mock of File authorization/grant/storage metadata or auth owner APIs | ASP.NET Core; EF Core; PostgreSQL; configured File storage; production Angular for browser path | `functional-fast` representative, `functional-full`, `functional-extended`, projected to `functional-release` | `tests/ui/run-real-backend-p0.mjs`: `TASK-V1-PR03C ... File grant reauthorization`; manifest MBJ-09 file grant reauthorization; #464 consumes authorized Project File; #481 probes revoked File | `B2` | `IMPLEMENTED` |
| `FUNC-FILE-002` | A user can upload/browse/select/preview or download an authorized File and observe persistence after navigation/reload | Authorized Workspace/Project; deterministic synthetic file content; isolated migrated DB and storage | upload -> browse/search -> open/preview/download -> navigate away/back or reload -> file remains correctly authorized | forbidden/oversized/invalid/revoked File path fails safely; no foreign metadata | `files` | No mock of File CRUD, grant, storage, auth, or persistence APIs | production Angular; ASP.NET Core; EF Core; PostgreSQL; File storage | `functional-full`, `functional-extended`, projected to `functional-release` via #482 | `tests/ui/angular-smoke.spec.ts` provides focused/mock UI coverage only; FCI-05 (#591) owns the missing real journey | `B3` | `NOT_IMPLEMENTED` |
| `FUNC-MSG-001` | A user can open a direct conversation, send/read a message, and observe durable live-backed persistence | Two synthetic authorized users/conversation fixture; migrated isolated DB | open/create DM -> send -> read/reload -> message remains and recipient state reconciles | unauthorized recipient/conversation access denied; stale/revoked audience cannot read protected content | `messaging` | No mock of conversation/message/auth/persistence APIs; realtime transport may fall back to real HTTP path when explicitly asserted | production Angular; ASP.NET Core; EF Core; PostgreSQL; SignalR or defined HTTP fallback | `functional-fast` representative, `functional-full`, `functional-extended`, projected to `functional-release` via #482 | `tests/ui/run-real-backend-p0.mjs` manifest MBJ-08 direct-message live-backed persistence; manifest MBJ-07 real HTTP fallback when Hub unavailable | `B2` | `IMPLEMENTED` |
| `FUNC-NOTIF-001` | A user receives only currently authorized Notifications and read/current-route state reconciles after delivery or revocation | Synthetic task/message event and authorized recipient; migrated isolated DB; durable notification worker where required | event -> durable delivery/reconciliation -> notification opens current route -> read/cleared state persists | revoked audience, stale authorization, invalid route target, or inaccessible object is suppressed/cleared | `notification/read-state` | No mock of notification authorization, persistence, route resolution, or owner business API | ASP.NET Core; EF Core; PostgreSQL; notification worker; production Angular | `functional-fast` representative, `functional-full`, `functional-extended` | `tests/ui/run-real-backend-p0.mjs`: `TASK-V1-PR07-D reauthorizes notification delivery, opens current Task routes, and clears revoked state through the real backend`; `Issue #378 queues durable immediate delivery, worker-publishes after reauthorization, and retains a revoked audience` | `B3` | `IMPLEMENTED` |
| `FUNC-AUTHZ-001` | Anonymous, normal-member, and administrator boundaries produce the correct allow/deny behavior and CSRF is enforced on unsafe requests | Synthetic SystemAdmin + invited Member; isolated migrated DB | admin-only endpoint allowed to admin; member protected endpoint allowed where authorized | anonymous -> 401; non-admin admin read/mutation -> 403; missing CSRF -> 403; logout -> session 401 | `cross-scope authorization/privacy` | No mock of auth, role, CSRF, or protected API | ASP.NET Core auth/authorization; PostgreSQL; CSRF/session | `functional-fast`, `functional-full`, `functional-extended` | `scripts/ci/run-mvp-a-authz-boundary-acceptance.sh`; `scripts/ci/mvp-a-authz-boundary-acceptance.sh`; MBJ-02/03 negative cases | `B2` | `IMPLEMENTED` |
| `FUNC-AUTHZ-002` | Losing Tenant/Workspace/Project/Task/File authority immediately removes protected access without leaking foreign metadata | Synthetic same-tenant and cross-tenant resources; revocable membership/file/source; isolated migrated DB | authorized read/mutation works before revocation; current-scope access remains valid | cross-Tenant/wrong-scope identifiers, revoked membership/file/source, stale authority deny with safe 401/403/404 semantics | `cross-scope authorization/privacy` | No mock of authorization, resource lookup, persistence, or protected API | ASP.NET Core; EF Core; PostgreSQL; File storage where relevant | `functional-fast` representative, `functional-full`, `functional-extended`, projected to `functional-release` | `TASK-V1-PR03C` revocation/File reauthorization; MBJ-02 cross-Tenant invite denial; #464 negative execution/source cases; #481 unauthorized Workspace/Project/Task/File projection | `B3` | `IMPLEMENTED` |

## P1 journeys

| Stable ID | User-visible goal | Preconditions / fixture | Positive path | Required negative path | Owner | Mock policy | Required dependencies | Expected gates | Existing test / script mapping | Budget | Current status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `FUNC-MSG-002` | A user can navigate channel/thread context and preserve expected message state across search/filter/navigation | Synthetic channel with multiple messages/users | open channel -> search/filter -> open thread/context -> return -> state is coherent | inaccessible channel/thread denied; stale cached protected content cleared | `messaging` | No mock of message/channel/auth/persistence owner APIs | production Angular; ASP.NET Core; EF Core; PostgreSQL; SignalR/HTTP fallback | `functional-extended`, optionally `functional-full`, projected to `functional-release` via #482 | `tests/ui/angular-smoke.spec.ts` focused/mock navigation coverage; #482 defines terminal cross-screen flow; FCI-06 (#596) owns missing real journey | `B3` | `NOT_IMPLEMENTED` |
| `FUNC-NOTIF-002` | Notification/read-state survives reload and realtime disconnect/reconnect without duplicate or unauthorized state | Synthetic notifications across read/unread and revocation states | receive -> mark/read/reconcile -> reload/reconnect -> durable state remains | duplicate/out-of-order event or revoked audience cannot resurrect protected unread state | `notification/read-state` | Core notification/read-state APIs and persistence real; transport failure injection is allowed only at transport boundary | production Angular; ASP.NET Core; PostgreSQL; SignalR/worker | `functional-extended`, optionally `functional-full` | current real P0 notification/reconciliation tests cover part of the contract; FCI-06 (#596) owns expansion | `B3` | `PARTIAL` |
| `FUNC-ANN-001` | An authorized user can create/edit/preview/review/schedule/publish an Announcement within implemented scope | Synthetic authorized author/reviewer and Workspace; deterministic time where scheduling is exercised | create -> edit -> preview/review -> publish/schedule -> audience sees current content | unauthorized author/reviewer/audience denied; invalid/stale schedule/action fails safely | `announcement` | No mock of Announcement/auth/persistence owner APIs; clock may be controlled through approved test seam | production Angular; ASP.NET Core; EF Core; PostgreSQL; scheduler if used | `functional-extended`, optionally `functional-full`, projected to `functional-release` via #482 | `tests/ui/angular-smoke.spec.ts` focused/mock UI coverage only; #482 owns terminal integrated regression; FCI-06 (#596) owns missing real journey | `B3` | `NOT_IMPLEMENTED` |
| `FUNC-AUDIT-001` | An authorized user can navigate/filter Audit evidence, open a redacted detail, and return without losing safe state/focus | Synthetic audit rows with sensitive fields; authorized and unauthorized users | filter/list -> open detail/deep link -> reveal allowed sensitive projection -> close/back -> state/focus retained | unauthorized sensitive metadata denied/redacted; injected content rendered as text; foreign audit metadata not disclosed | `audit/navigation` | No mock of Audit/auth/redaction/persistence APIs for owner credit | production Angular; ASP.NET Core; EF Core; PostgreSQL | `functional-extended`, optionally `functional-full`, projected to `functional-release` via #482 | `tests/ui/angular-smoke.spec.ts` has focused mocked 320px/redaction/focus coverage only; #482 terminal regression provides integrated downstream evidence | `B3` | `NOT_IMPLEMENTED` |
| `FUNC-XNAV-001` | Global navigation across major screens does not land on blank/404/redirect-loop states and preserves session boundary | Authorized synthetic user with representative Workspace/Project data | navigate Workspace -> Files -> Messages -> Project/Task -> Audit/Announcement where authorized | unauthorized routes deny/redirect safely; logout/tenant invalidation clears protected frontend state | `audit/navigation` (cross-screen integration owner) | Domain owner APIs must be real for release credit; focused static route smoke is non-owner | production Angular; ASP.NET Core; PostgreSQL; representative domain fixtures | `functional-extended`, `functional-release` | `tests/ui/angular-smoke.spec.ts` focused static route smoke; #482 is the intended integrated owner | `B4` | `NOT_IMPLEMENTED` (awaiting #482 completion) |

## Existing suite mapping summary

This section makes the required Issue #577 mappings explicit even when one
suite spans several stable journey IDs.

| Existing suite / evidence | Canonical role in this taxonomy | Journey IDs |
| --- | --- | --- |
| `tests/ui/angular-smoke.spec.ts` | `focused-mock-regression` + accessibility/responsive/visual regression. It is not the real-functional owner for intercepted core routes. | Supports `FUNC-TASK-002`, `FUNC-FILE-002`, `FUNC-MSG-002`, `FUNC-ANN-001`, `FUNC-AUDIT-001`, `FUNC-XNAV-001` as focused regression only |
| `tests/ui/run-real-backend-p0.mjs` + `scripts/ci/real-backend-pr-p0-required-tests.txt` | Current high-value Compose-backed real P0 slice; source material for future `functional-fast` and `functional-full` lanes | `FUNC-AUTH-001`, `FUNC-WS-001`, `FUNC-PROJ-001`, `FUNC-TASK-001`, `FUNC-FILE-001`, `FUNC-MSG-001`, `FUNC-NOTIF-001`, `FUNC-AUTHZ-002` |
| `tests/ui/run-real-backend-my-tasks.mjs` | Real My Tasks owner | `FUNC-TASK-002` |
| MBJ-01 acceptance | Fresh bootstrap and persisted default Workspace | `FUNC-AUTH-003`, secondary evidence for `FUNC-WS-001` |
| MBJ-02 acceptance | Invite onboarding plus used/revoked/expired/mismatch/cross-Tenant negatives | `FUNC-AUTH-002`, `FUNC-AUTHZ-002` |
| MBJ-03 acceptance | Password/session lifecycle, CSRF, restart persistence, suspension/reactivation, expiry | `FUNC-AUTH-001` |
| `scripts/ci/run-mvp-a-authz-boundary-acceptance.sh` | Representative real authorization/CSRF/logout boundary | `FUNC-AUTH-001`, `FUNC-AUTHZ-001` |
| #464 Task execution Golden Path | Complete Compose-backed execution/source/durable-result owner | `FUNC-EXEC-001`, secondary evidence for `FUNC-FILE-001` and `FUNC-AUTHZ-002` |
| #481 Public HTTPS Production Golden Path | Release-only public TLS/proxy/cookie/CSRF deployment projection. It does not replace Compose domain owners. | Projects `FUNC-AUTH-001`, `FUNC-WS-001`, `FUNC-PROJ-001`, `FUNC-TASK-001`, `FUNC-EXEC-001`, `FUNC-FILE-001`, `FUNC-AUTHZ-002` into `functional-release` |
| #482 Cross-screen Regression Gate | Integrated terminal cross-screen regression after domain changes land. It does not replace domain owner journeys. | Downstream evidence for `FUNC-FILE-002`, `FUNC-MSG-002`, `FUNC-ANN-001`, `FUNC-AUDIT-001`, `FUNC-XNAV-001`, plus representative core navigation |

## Owner-test rule for duplicate coverage

For each stable ID, only the matrix-designated real owner is allowed to satisfy
that journey in a blocking Functional gate. Broader tests may re-exercise the
same path, but are reported as `focused regression`, `negative expansion`, or
`release projection`.

Examples:

- #481 replays core auth/Workspace/Project/Task/File/execution through a public
  endpoint to prove deployment topology. It is not a second PR owner for those
  feature journeys.
- #482 spans many screens to detect integration regressions. It is not allowed
  to make a missing Files, Messaging, Announcement, or Audit owner test appear
  implemented.
- screenshot assertions inside `angular-smoke.spec.ts` may block their own
  visual regression contract, but they do not satisfy a real-functional stable
  ID.

## Change requirement

Any PR that changes a Functional test in a way that affects journey semantics,
owner status, mock boundaries, stable test titles/manifests, gate ownership, or
runtime budget must update this matrix in the same PR. New journeys receive a
new stable ID; existing IDs are never reassigned to a different user goal.
